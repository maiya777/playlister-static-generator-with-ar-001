<!DOCTYPE html>
<html>
<head>
    <title><%= @artist.name%></title>
  </head>
   <body>
    <ul>
      <% @artist.songs.each do |song|%>
      <li><a href="songs/<%= song.to_slug%>.html"><%= song.name%></a></li>
      <% end%>
      <% @artist.genres.each do |genre|%>
      <li><a href="genres/<%= genre.to_slug%>.html"><%= genre.name%></a></li>
      <% end%>
      </ul>
      
  </body>
</html>