<!DOCTYPE html>
<html>
<head>
    <title>Artists</title>
  </head>
   <body>
    <ul>
      <%Artist.all.each do |artist|%>
      <li><a href="artists/<%= artist.to_slug%>.html"> <%= artist.name%></a></li>
      <%end%>
    </ul>
  </body>
</html>